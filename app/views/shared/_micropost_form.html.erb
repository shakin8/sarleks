<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_area :content, placeholder: "Share your activities..." %>
  <%= f.submit "Post", class: "button" %>
  <span class="countdown"></span>
<% end %>
